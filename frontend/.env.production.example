# Firebase Hosting — informational landing only
REACT_APP_SSC_LANDING_ONLY=true

# Public site metadata (shown on landing page)
REACT_APP_SSC_VERSION=0.3.1
REACT_APP_SSC_BUILD=14
REACT_APP_SSC_RELEASE_TAG=v0.3.1

# Installed client production build (Electron / Android WebView) — use LANDING_ONLY=false
# REACT_APP_API_URL=https://api.supersecurechat.com
# REACT_APP_SSC_PLATFORM=electron
# REACT_APP_SSC_REQUIRE_LIBCRYPTO=true

# Cloudflare Turnstile (register / recovery / feedback when SSC_CAPTCHA_REQUIRED=true on API)
# REACT_APP_TURNSTILE_SITE_KEY=REPLACE_WITH_TURNSTILE_SITE_KEY
# TASK J founder QA — smashmaxxx (Windows) ↔ dots (Android) — 27 Jun 2026

## Reported issues → root cause → fix

| # | Symptom | Root cause | Fix (code) |
|---|---------|------------|------------|
| 1 | PC UI in Spanish | `user.language` / OS locale applied to installed UI | Installed clients default UI **English** until user picks language in Settings |
| 2 | Android welcome 1/3, PC none | Onboarding key was per-user only (phone completed → same user on PC skipped) | Onboarding key now **per device** (`desktop` / `android` / `web`) |
| 3 | Text/file/voice send fails | Signal session not ready; errors swallowed; panic left stale libsignal store | Stricter `prepareInstalledMessaging`; panic **wipes libsignal store**; clearer errors |
| 4 | Calls not received on PC | Encrypted call signaling decrypt failed silently (`catch return`) | Warm X3DH session before unpack; cleartext SDP fallback when decrypt fails |
| 5 | Panic → scary banner for wife | Redirect was `/login?panic=1` (persistent URL banner) | Redirect plain `/login`; one-time subtle self-ack in sessionStorage only |
| 6 | Cloudflare Turnstile “Success” on installed | Widget rendered on APK/desktop login | Hidden on installed clients; API skips captcha with `X-SSC-Client: installed` |
| 7 | “Push notifications enabled” spam | `subscribePush` + `subscribeNativePush` toasts on every login | Silent registration once per session |
| 8 | “Signed in with Google” toast | GoogleAuthCallback success toast | Removed |
| 9 | In-app toasts feel wrong | Sonner toasts used for system events | Push/call toasts reduced; **Windows has no FCM** — calls need app open + WebSocket |

## Retest checklist (after v1.0.10 rebuild)

1. **PC language** — fresh install or clear app data → UI should be **English**
2. **Onboarding** — both Android and Windows should show **Welcome 1/3** once per device
3. **Messaging** — wait ~5s after opening chat (Signal bootstrap) → text, image, voice note
4. **Calls** — PC app **in foreground** → phone calls PC → incoming UI + ringtone
5. **Panic** — wipe on phone → reopen → **no** “emergency wipe complete” banner for next opener
6. **Login** — no Turnstile widget on installed login screens

## Known limits (not bugs)

- **Windows desktop** does not receive FCM; incoming calls/notifications when PC app is **closed** are not supported yet
- **Off-LAN video** still depends on TASK P.6 TURN verification